Git spurningar og svör
1. Hvað gera eftirfarandi Linux skipanir?
	• cd  - The cd command is used to change the current directory
	• cd ~ - Changes to the home directory
	• ls - Lists directory contents of files and directories
	• pwd - Writes the full pathname of the current working directory
	• mkdir - Creates a new directory

2. Hvað gera eftirfarandi git skipanir og hvers vegna eru þær gagnlegar?
	• git clone - Used to make a local copy of a remote repository
	• git log - Shows the commit logs
	• git status - Show you information about the current state of your files in a git repository and highlight any differences from the last time you used 'git commit'.
	• git diff - Show you line-for-line differences in your files.
	• git checkout - Its used to create branches, switch branches, and checkout remote branches.

3. Hver er munurinn á eftirfarandi git skipunum:
	a) git diff  - Will show changes between commits
	b) git diff --staged - This form is to view the changes you staged for the next commit relative to the named
	c) git diff commit1 commit2 - This is to view the changes between two arbitrary

4. 	Hvað er útgáfustýring (e.version control)? 
	• Its a component of software configuration management

5.	Hverjir eru helstu kostir við að nota GIT?
	• Branching and merging are easy
	• Workflow is flexible
	• Data integrity is assured
	• Free

6.	Hversu oft telur þú að eigi að gera commit í verkefni, rökstuddu?
	- Mér finnst að það ætti að koma fyrir við hvertja spurningu / verkefni afþví það vinna ekkert allir eins og það gerir það að verkum að verkefnin eru ekkert alltaf skiljanleg fyrir þann sem fer yfir.

7.	Hvað er "Working directory"?" "Staging area"?" og "Repository" í GIT?
	• Working directory - Its a single checkout of one version of the project
	• Staging area - A cache of files that you want to commit
	• Repository - Is a data structure that stores projects or a set of files